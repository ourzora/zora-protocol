---
"@zoralabs/coins-sdk": patch
---

Add new Valuation fields to coin balances endpoint, coin price history endpoint, and creator livestream comments endpoint

Only works with api-key authenticated calls otherwise valuation response is null.
