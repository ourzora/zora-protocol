---
"@zoralabs/cli": patch
---

Make `zora agent create` mint the creator coin automatically by default.

Pass `--skip-coin` to skip it, or run `zora agent coin` to mint it for an existing agent at any time.
