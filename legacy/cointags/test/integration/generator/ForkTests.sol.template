// SPDX-License-Identifier: MIT
pragma solidity ^0.8.23;

// auto-generated by running `pnpm run generate`

import "forge-std/Test.sol";
import { CoinPoolBase } from "../CoinPoolBase.sol";

contract TestCoinsSwap is CoinPoolBase {
    {{#each coins}}
    function test_forkBuyBurnExisting_{{chainName}}_{{contractAddress}}() external {
        vm.createSelectFork("{{chainName}}");
        _testDeployedCointagCreateAndBurn(address({{poolAddress}}), "{{coin}}", "{{version}}");
    }

    function test_forkBuyBurnNew_{{chainName}}_{{contractAddress}}() external {
        vm.createSelectFork("{{chainName}}");
        _testNewCointagCreateAndBurn(address({{poolAddress}}), "{{coin}}", "{{version}}");
    }
    {{/each}}
}
