// Fallback ABI for environments that do not have Foundry (forge) available.
// The full generated ABI is produced by `pnpm run wagmi:generate` when forge is installed.
export const zoraAccountManagerImplABI = [] as const;
